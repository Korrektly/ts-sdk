{
	"name": "@korrektly/sdk",
	"module": "src/index.ts",
	"type": "module",
	"scripts": {
		"test": "bun test",
		"check": "bunx biome check --write",
		"biome:ci": "bunx biome ci",
		"publish-packages": "bun run build --filter=@korrektly/sdk && changeset version && changeset publish"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.0",
		"@changesets/cli": "^2.29.7",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
